[package]
name = "hello-raw"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"

# Guest profile: small, panics become wasm traps (the host's failure policy
# handles them).
[profile.release]
opt-level = "s"
panic = "abort"
